<%= form_for @comment  do |f| %>
    <%= f.hidden_field :user_id %>
    <%= f.hidden_field :figure_id %>
    <%= f.label :comment  %><br>
    <%= f.text_area :comment, :id => "message_area", :cols => 50, :rows => 10 %><br>

    <%= f.submit%>

   
    <%end%>